/*
 * graphics.c — stub required by the LCF framework.
 *
 * This translation unit is compiled with -O0 so the LCF call-tracer can
 * instrument it.  All real rendering is in view/view.c.
 */
int graphics_stub(void) { return 0; }
