#ifndef __PROJ_OBSTACLE_XPM_H
#define __PROJ_OBSTACLE_XPM_H

#include <lcom/xpm.h>

// Set to 1 and fill in the array below when the sprite is ready.
#define HAS_OBSTACLE_XPM 0

#if HAS_OBSTACLE_XPM
static xpm_row_t obstacle_xpm[] = {
    /* paste XPM data here */
};
#endif

#endif /* __PROJ_OBSTACLE_XPM_H */
